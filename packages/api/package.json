{
  "name": "@feluda/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Feluda API — Fastify server that wires the core layers (Layer I: API/CLI).",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsup",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/rate-limit": "^9.1.0",
    "@feluda/core": "*",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.0"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "tsx": "^4.15.0",
    "typescript": "^5.5.0"
  }
}

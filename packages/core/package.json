{
  "name": "@feluda/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Feluda core — the 7 architecture layers as composable modules.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "docx": "^8.5.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
